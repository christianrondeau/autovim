#!/usr/bin/env bash
C=''
for i in "$@"; do 
  i="${i//\\/\\\\}"
  C="$C\"${i//\"/\\\"}\","
done
A="let g:autovim_cmd=[$C\"\"]"
B="exec vim -u ${0%/*}/../src/vimrc.vim -S ${0%/*}/../src/autovim.vim --not-a-term --cmd '$A'"
eval "$B"
